services:
  subarr:
    build: .
    image: subarr:local
    ports:
      # Host 7979 → app; use 7980 on the host only if you run a second mapping or reverse proxy (see unraid/subarr.xml)
      - "7979:3001"
    environment:
      PORT: "3001"
      NODE_ENV: production
      SUBARR_DB_PATH: /config/subarr.db
    volumes:
      # Unraid defaults — adjust for your machine
      - /mnt/user/appdata/subarr:/config
      - /mnt/user/Youtube:/downloads
    restart: unless-stopped
